---
export interface Props {
  href: string
  text: string
  className?: string
}

const { href, text, className = '' } = Astro.props
---

<a href={href} class={`text-gray-900 hover:text-sick-blue font-medium transition-colors ${className}`}>
  {text}
</a>

